import { test } from '@playwright/test';

test.describe('Importateur Demande Renouvellement', () => {
  test('IMP-DEM-REN-002 | Renouvellement with autre numero certificat', async () => {
    test.skip(true, 'Planned scenario scaffold. Implement renouvellement flow with autre N° de certificat.');
  });
});
